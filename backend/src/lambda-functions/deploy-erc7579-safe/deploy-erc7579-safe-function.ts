// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for DeployErc7579SafeFunction
 */
export interface DeployErc7579SafeFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/lambda-functions/deploy-erc7579-safe/deploy-erc7579-safe.
 */
export class DeployErc7579SafeFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: DeployErc7579SafeFunctionProps) {
    super(scope, id, {
      description: 'src/lambda-functions/deploy-erc7579-safe/deploy-erc7579-safe.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs18.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/lambda-functions/deploy-erc7579-safe/deploy-erc7579-safe.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}